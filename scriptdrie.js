const getal = function (number1, number2, number3, number4, number5) {
    const sum = number1 + number2 + number3 + number4 + number5;
    Math.round;
    return sum;
};

const result = getal(4, 5, 3, 1, 2);
console.log(result);